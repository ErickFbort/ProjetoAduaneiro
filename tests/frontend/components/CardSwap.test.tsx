import { render, screen } from '@testing-library/react';
import { CardSwap } from '../../src/components/Cards/CardSwap';

describe('CardSwap Component', () => {
  it('renders without crashing', () => {
    render(<CardSwap />);
    // Add more specific tests as needed
  });
});
